# 文档定义
--该文档是为改善AI和用户的和作质量而撰写
## 目的
--创建结构清晰，层次合理，性能稳定，能重复使用的Python项目。
--逐步定义AI和用户的沟通方式

## 沟通方式
--尽量用中文

## 思考模式
--从各方面，包括但不限于上下文，理解用户的真实需求
--简洁总结需求，反馈给用户已验证
--思考从具体到到抽象，创建从抽象到具体
--自上而下来写项目架构，确认没问题后，实施细节补充
--问题-问题定义-目标-抽象实现-具体代码
--具体函数代码，要检验，验证无误后在进行下一步


## 项目说明
--- 这是一个以Genmni API为中心构建的项目
--- 核心功能通过 FUNTION CALL 功能实现
--- 技术细节 务必参照 gemini_usage_guide.md 文件，请务必理解Genmi API的使用方法
--- 项目的主要功能是根据用户的需求，创造，保存，重复利用符合要求的函数以供其他AI调用
--- 实现方式 Agent1发现需要调用函数满足，他分析用户的最终目的和实现方式，并将详细内容，以结构化的方式告诉Agent2，Agent2会检验是否有符合条件的函数，如果没有，就创造如果有，就发给worker，worker正常调用后，反馈结果给Agent1，函数注册到tools，不行则反馈给Agent2，让其重新考率，创造。Agent3知道Agent1的工作内容，主要目的是提高函数的可用性，有权合并，删除，更新函数，拥有tools的最高管理权限。

## 项目配置
--project.toml
--git 地址:git@github.com:liguanggeng/cgemini.git
--docker

## 其他
## Agent2 重点强化
--角色定位：负责检索或生成可直接调用的函数，并输出符合 Gemini function call 规范的声明。
--流程要求：先检索现有 tools；若无匹配函数，再制定新实现；设计阶段需列出参数类型、输入校验策略与边界处理方案；生成后提供示例调用及预期结果。
--质量标准：函数需可重入、避免隐式副作用（如不可避免需在声明中明确）；遵循项目的命名、异常处理与日志规范；每个函数至少给出一项自动化验证方案（单元测试、doctest 等）。
--输出清单：统一给出 `name`、`description`、`args` JSON Schema、`returns` 描述、`requirements`（依赖或环境）、`validation`（测试方式）、`ownership`（责任人或 Agent2 版本标签）。
--反馈闭环：worker 执行结果或错误需原样反馈给 Agent1；失败时先根据日志自查并修订，再次提交更新版本并记录修改点。

## Agent1 优化方向
--角色定位：聚焦梳理用户真实需求与上下文，禁止直接编写函数实现。
--信息采集：明确目标、输入输出、约束、已有资源，缺失项需列成清单。
--澄清回路：对于不明确信息，先向用户提问，得到答案后更新任务概要。
--输出格式：对 Agent2 的交付需按 `context`、`objective`、`requirements`、`constraints`、`tooling` 顺序结构化呈现，必要时标注“待确认”。
--质检要点：使用中文、保证简洁但不遗漏关键条件，区分事实与推测，并在需求发生变化时实时更新。

## Agent3 治理要点
--角色定位：担任函数资产管理员，负责审批、合并、下线与版本记录。
--审查流程：对新函数进行命名、参数一致性、依赖、测试覆盖与文档同步检查；给出 `approve`、`revise` 或 `reject` 结论并说明理由。
--资产维护：维护函数清单（含状态、版本、依赖、验证记录），定期清理冗余并跟踪性能或缺陷情况。
--沟通风格：中文输出、客观中立，引用具体字段或行号指出问题，必要时向 Agent1/Agent2 提出改进建议。

## Gemini 使用要点补充
--统一通过 `google-genai` 客户端调用，可按需选择 Developer API 或 Vertex AI，并确保 API key、project、location 配置齐备。
--文本生成支持同步、流式与异步三种模式，可在 `GenerateContentConfig` 中设置系统指令、温度、采样参数与安全策略。
--多模态输入可使用 `types.Part` 传递本地或 GCS 文件，文件上传需走 Developer API 的 `client.files.upload` 接口。
--函数调用可自动或手动声明，声明需包含参数 schema；执行后结果写入 tools 注册表，便于后续复用。
--常用模型、嵌入、图像与视频生成能力见 `gemini_usage_guide.md`，调用前确认 Token 限制与适配场景。


## 沟通风格与报告
-- 经用户确认，这是一种有效的沟通风格，应在未来工作中遵循。
1.  **故事化叙事 (Storytelling)**：当报告复杂流程（如多代理协作）的结果时，应采用故事或剧本的形式（例如，第一幕、第二幕...）来叙述，使过程更生动、易于理解。
2.  **解码与简化 (Decode and Simplify)**：对于原始的、包含转义字符的计算机输出（如JSON），必须进行解码和格式化，以清晰、美观、人类可读的方式呈现给用户。
3.  **突出关键洞察 (Highlight Key Insights)**：不要仅仅陈述最终结果（如“成功”或“失败”）。应深入分析执行日志，解释‘为什么’会产生这个结果，并重点突出系统中的关键交互（如审查、修正）和决策点。
4.  **关联项目目标 (Connect to Project Goals)**：始终将执行结果与项目的核心目标联系起来，向用户展示当前的工作如何服务于更大的蓝图。
